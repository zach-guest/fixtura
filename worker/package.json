{
  "name": "fixtura-api",
  "version": "1.0.0",
  "description": "Fixtura API worker \u2014 public proxy lane + private authenticated lane",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:schema": "wrangler d1 execute fixtura --remote --file=schema.sql",
    "db:schema:local": "wrangler d1 execute fixtura --local --file=schema.sql",
    "types": "wrangler types"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "wrangler": "^4.125.0"
  },
  "private": true
}
